name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.5
      - run: bun install --frozen-lockfile
      - run: bun run lint
      - run: bun run typecheck
      - run: bun run test --coverage
      - run: bun run build
      - name: Verify tag and build tarball
        run: |
          PACKAGE_VERSION="$(node --print "require('./package.json').version")"
          test "$GITHUB_REF_NAME" = "v${PACKAGE_VERSION}"
          bun pm pack
          PACKAGE_TARBALL="dsh-sonarqube-${PACKAGE_VERSION}.tgz"
          tar --list --gzip --file "$PACKAGE_TARBALL"
          cp "$PACKAGE_TARBALL" dsh-sonarqube.tgz
          sha256sum "$PACKAGE_TARBALL" dsh-sonarqube.tgz >SHA256SUMS
          echo "PACKAGE_TARBALL=$PACKAGE_TARBALL" >>"$GITHUB_ENV"
      - name: Publish GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: >-
          gh release create "$GITHUB_REF_NAME" "$PACKAGE_TARBALL" dsh-sonarqube.tgz SHA256SUMS
          --verify-tag --generate-notes
